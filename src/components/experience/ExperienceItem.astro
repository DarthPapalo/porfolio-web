---
interface Props {
    title: string;
    start_date: string;
    end_date: string;
    location: string;
}

const { title, start_date, end_date, location } = Astro.props;
---

<article class={"bg-background-light/70 dark:bg-background-dark/70 p-6 rounded-2xl border-2 border-foreground-light/80 dark:border-foreground-dark/80"}>
    <h3 class="text-2xl text-foreground-light dark:text-foreground-dark">{title}</h3>
    
    <div class="flex row justify-between">
        <p class="text-lg text-foreground-light/70 dark:text-foreground-dark/70 italic mb-1.5">{start_date} - {end_date}</p>
        <p class="text-lg text-foreground-light/70 dark:text-foreground-dark/70 italic mb-1.5">{location}</p>
    </div>

    <slot />

</article>