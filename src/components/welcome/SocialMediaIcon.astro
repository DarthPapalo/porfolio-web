---
interface Props {
    url: string;
}

const { url } = Astro.props;
---

<a class="dark:stroke-secondary-dark size-[50px] hover-effect" href={url} target="_blank" rel="noopener noreferrer">
    <slot />
</a>
