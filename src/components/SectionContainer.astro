---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<section>
    <header class="flex justify-center mb-10">
        <h2 class="text-2xl md:text-4xl lg:text-5xl text-foreground-light dark:text-foreground-dark">{title}</h2>
    </header>
    <slot />
</section>